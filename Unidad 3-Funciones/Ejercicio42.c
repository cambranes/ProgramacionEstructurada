#include <stdio.h>
#include <math.h>

//Prototipos de las funciones
float num = 0;
float numRaiz= 0 ;
float raizProceso(float,float);
//Salida 
int main() {
	
	raizProceso(num,numRaiz);
	
return 0;
}
//Funciones
float raizProceso(float num,float numRaiz){
	//Se obtiene la raiz
	for (num=0; num<=100; num++){
		numRaiz=sqrt(num);
		return num;
		return numRaiz;
	}
	return num;
	return numRaiz;
}
	
